extends ../base
include ../mixins/video

block content
    div.main__video-grid
        h1 ##{hashtag}
        div.video-grid
            each video in videos
                +video(video)
            else
                span.empty__message No hashtag found